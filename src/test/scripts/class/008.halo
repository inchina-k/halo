class Point:
    var x;
	var y;

    fun _init_(x, y):
        let my.x = x;
        let my.y = y;
    end
	
    fun dist_sqr(other):  
        return my.x;
    end
end

var p = Point(1, 2);
println(to_str(p));

let p.x = 42;
println(to_str(p));

println(p.x + p.y);