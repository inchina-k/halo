class Point:
    var x;
	var y;

    fun _init_(x, y):
        let my.x = x;
        let my.y = y;
    end
	
    fun move(dx, dy):  
        let my.x = my.x + dx;
        let my.y = my.y + dy;
    end
end

var p = Point(3, 4);
println(p);

println(p.x);
println(p.y);

p.move(0, -1);
println(p);

println(Point);