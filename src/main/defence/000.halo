# INSERTION SORT

var lst = [5, 1, 3, 2, 4, 0, 1, 1];

for i in (1, lst.len()):
    var k = lst[i];
    var j = i - 1;

    while j > -1 and lst[j] > k:
        let lst[j + 1] = lst[j];
        let j = j - 1;
    end

    let lst[j + 1] = k;
end

println(lst);